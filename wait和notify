这两个方法定义在Object类中的方法.
wait()等待:让持有这个对象锁的线程,立即释放这个锁标记,进入等待队列.
注意:等待队列中的线程,是不能参与锁标记的争夺的.
notify()唤醒:唤醒等待队列中的任意一个等待某个锁标记的线程.这个被唤醒的线程会从等待队列中进入到锁池,可以参与锁标记的争夺.
notify()唤醒等待队列中的所有等待指定标记的线程.这些被唤醒的线程会从等待队列中进入到锁池,参与锁标记争夺.
例子我们放到经典的消费者和生产者模式当中.


